#include<stdio.h>

int main()
{
    char ch= 0X40; //if it's not enclosed in '' then the digit will be considered as a integer 
    printf("ch in char is %c\n",ch);
    printf("ch in decimal is %d\n",ch);
    return 0;
}